export enum VerifyTarget {
    PHONE = "PHONE",
    EMAIL = "EMAIL",
}

export enum RideStatus {
    ACCEPTED = "ACCEPTED",
    FINISHED = "FINISHED",
    CANCELED = "CANCELED",
    REQUESTING = "REQUESTING",
    ONROUTE = "ONROUTE",
}
